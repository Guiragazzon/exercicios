//Array de Números: Crie um array com números inteiros e use um loop para exibir cada número no console.

let numeros = [10, 20, 30, 40, 50];

for (let i = 0; i < numeros.length; i++) {
  console.log(numeros[i]);
}
